#include <iostream>
#include <cmath>
using namespace std;

int sumPowers(int b, int n) {
    if (n == 0) return 1;
    return pow(b, n) + sumPowers(b, n - 1);
}
int main() {
    int b = 4, n = 3;
    cout << sumPowers(b, n) << "\n";
    return 0;
}
